# Comparison

For performance and feature comparisons, refer to the benchmarks page and API reference.

jsonpathx is a single-package, JavaScript-only implementation. Historical references to separate packages (CLI, compat, bindings) are no longer applicable.
