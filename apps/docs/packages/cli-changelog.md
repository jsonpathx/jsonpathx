# @jsonpathx/cli (Archived)

This repository currently ships a single package: `jsonpathx`.

The standalone CLI package is not part of this codebase anymore. If you need a CLI, run JSONPath queries from your own scripts using `jsonpathx` or ask to add a minimal CLI here.
