# jsonpathx Core

This repository ships a single package: `jsonpathx`.

Core functionality, QueryBuilder, caching, streaming, and utilities are all part of the main package. See `/api/jsonpath`, `/api/query-builder`, and `/api/utilities` for the current API reference.
