# @jsonpathx/graphql (Archived)

This repository currently ships a single package: `jsonpathx`.

The standalone GraphQL integration package is not part of this codebase anymore. If you need GraphQL helpers, we can add a small set of utilities inside `jsonpathx` or publish a separate package later.
